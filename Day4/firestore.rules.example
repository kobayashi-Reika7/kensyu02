// Firestore セキュリティルールの例（DEBUG_FIRESTORE.md ⑥ 準拠）
// Firebase Console → Firestore → ルール にコピーして使用
//
// 最短で直す: 下のルールを Firebase Console に貼り付け → 「ルールを公開」をクリック
//
// ========== 【開発用】すべて許可 ==========
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /lists/{listId} {
      allow read, write: if true;
    }
    match /todos/{todoId} {
      allow read, write: if true;
    }
  }
}
//
// ========== 【日付制限付き】推奨（本番前は日付を延長） ==========
// rules_version = '2';
// service cloud.firestore {
//   match /databases/{database}/documents {
//     match /lists/{listId} {
//       allow read, write: if request.time < timestamp.date(2026, 12, 31);
//     }
//     match /todos/{todoId} {
//       allow read, write: if request.time < timestamp.date(2026, 12, 31);
//     }
//   }
// }
