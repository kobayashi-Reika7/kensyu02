# Firebase 設定（このファイルを .env にコピーし、値を入れてください）
# Vite では VITE_ プレフィックス付きの変数のみクライアントに公開されます
#
# 【重要】
# - .env は GitHub に push しない（API キー等が含まれるため）
# - .env.example は push して OK（値は空のテンプレート）
# - .env を変更したら npm run dev を再起動すること（反映のため）

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# クラウドデプロイ時のみ: バックエンド API のベース URL（未設定時は http://localhost:8000）
# 例: https://your-api.railway.app
# VITE_API_BASE=
